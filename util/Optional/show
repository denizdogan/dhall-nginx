let Optional/fold = https://prelude.dhall-lang.org/Optional/fold in
  λ(type : Type)
→ λ(extractionFunction : type → Text)
→ λ(value : Optional type)
→ "${Optional/fold type value Text extractionFunction ""}"
